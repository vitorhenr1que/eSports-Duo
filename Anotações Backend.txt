npm i prisma -D  //Dev
npx prisma init --datasource-provider SQLite
npx prisma migrate dev // Criar Banco de Dados (após rodar o comando pede pra digitar algo tipo commit)
instalar extensões: Prisma no VsCode
npx prisma studio // Visualizar DB no navegador



// Dentro de schema.prisma
model NomeDaTable {

id String @id  // @default(uuid()) para gerar ids automaticos (Existem outros também, ctrl+space para ver)
nome TypeQueQuiser
idade Number
API Json
data DateTime @default(now())

}

----- Importar Prisma pra Aplicação -------
1- npm i @prisma/client  // para poder adicionar  prisma ao código express.js/ts
2- import { PrismaClient } from '@prisma/client'
3- const prisma = new PrismaClient()
4- const games = prisma.nomeTable.findMany()
